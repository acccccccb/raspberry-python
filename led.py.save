import RPi.GPIO as GPIO
import time

LED = 26
count = 0
GPIO.setmode(GPIO.BCM)
GPIO.setup(LED, GPIO.OUT)

try:
	while ( count < 3 ) :
	GPIO.output(LED, GPIO.HIGH) time.sleep(0.2)
    GPIO.output(LED, GPIO.LOW)
    time.sleep(0.2)
    #count = count + 1

except KeyboardInterrupt:
    GPIO.output(sound,GPIO.HIGH)
    GPIO.cleanup()
